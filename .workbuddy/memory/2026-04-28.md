# 2026-04-28 Omni-Search 稳定性审修

完成 GPT 5.5 审查的 9 项修复，全部通过测试(38/38)和 build：
1. useOmniSearch 竞态修复 — abortRef guard 防止旧请求覆盖新状态
2. href encodeURIComponent — safeSlug() 辅助函数 + 空slug过滤
3. isInternalHref + action await + try/catch — executeItem 安全化
4. a11y — aria-controls/aria-activedescendant/aria-expanded/aria-autocomplete + option id
5. focus trap — Tab/Shift+Tab wrap in dialog
6. Tailwind bg-[var(--color-accent)]/12 → color-mix inline style
7. useOmniHotkeys — e.key.toLowerCase() + IME composition guard
8. 清理 — 移除 useCallback 未用 import
9. 测试 — 新增 slug encoding, isInternalHref, stale request guard, aria ID, hotkey case

修改文件：useOmniSearch.ts, OmniPalette.tsx, OmniSearchInput.tsx, OmniResultItem.tsx, useOmniHotkeys.ts, tests/omni-search.test.ts
