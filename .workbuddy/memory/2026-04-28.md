# 2026-04-28

## Omni-Search 全局命令面板实现

为 Vocab Observatory 实现了完整的 Omni-Search 命令面板功能。

- **新增文件**: components/omni/ 下 11 个文件（types.ts, useOmniStore.tsx, omni-actions.ts, useOmniSearch.ts, useOmniHotkeys.ts, OmniSearchInput.tsx, OmniResultItem.tsx, OmniSection.tsx, OmniFooter.tsx, OmniPalette.tsx, index.tsx）
- **修改文件**: app/layout.tsx（注入 OmniPalette）
- **测试**: tests/omni-search.test.ts（19 个测试全部通过）
- **文档**: docs/omni-search.md
- **新增依赖**: 无
- **关键决策**: 
  - React Context + useReducer 替代 Zustand（不新增依赖）
  - client wrapper index.tsx 解决 Turbopack 不允许 Server Component 中 `next/dynamic` + `ssr:false` 的问题
  - `router.push(item.href as Parameters<typeof router.push>[0])` 解决 Next.js 16 RouteImpl 类型约束
  - 主题切换命令独立实现 cycleTheme 逻辑，不依赖 ThemeToggle 组件实例
  - 动态搜索通过 debounce 150ms + AbortController 调用 /api/words 和 /api/plaza
- **Build**: `npx next build` 通过
